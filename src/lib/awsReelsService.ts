// Minimal AWS Reels service stub — no-op implementations to keep imports working
export const getReelById = async (id: string) => {
  return null;
};

export const updateReel = async (id: string, data: any) => {
  return null;
};

export default {
  getReelById,
  updateReel,
};
