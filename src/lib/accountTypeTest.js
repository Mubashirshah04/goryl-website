// ✅ AWS DYNAMODB - Firestore completely removed
// Account type test .js - AWS stubs

export const testAccountType = async (userId) => {
    console.warn('⚠️ testAccountType (.js): AWS implementation pending');
    return { accountType: 'user', verified: false };
}
