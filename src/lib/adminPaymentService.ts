// ✅ AWS DYNAMODB - Firestore completely removed
// Admin payment service - AWS stubs

export const getAdminPayments = async () => {
  console.warn('⚠️ getAdminPayments: AWS implementation pending');
  return [];
}

export const processRefund = async (paymentId: string, amount: number) => {
  console.warn('⚠️ processRefund: AWS implementation pending');
}
