// ✅ AWS DYNAMODB - Firestore completely removed
// Account type test - AWS stubs

export const testAccountType = async (userId: string) => {
  console.warn('⚠️ testAccountType: AWS implementation pending');
  return { accountType: 'user', verified: false };
}